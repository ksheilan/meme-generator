'use strict'

function onInit(){
    onRenderMeme()
    onRenderGallery()
    onClearCanvas()
    // window.addEventListener('resize', () => {
    //     resizeCanvas()
    //     // drawTriangle(50, 250)
    // })
}