'use strict'

function onInit(){
    addListeners()
    onCreateNewLayer()
    onRenderMeme()
    onRenderGallery()
    onClearCanvas()
    // window.addEventListener('resize', () => {
    //     resizeCanvas()
    //     // drawTriangle(50, 250)
    // })
}