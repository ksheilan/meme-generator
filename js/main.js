'use strict'

function onInit(){
    addListeners()
    onRenderMeme()
    onRenderGallery()
    onClearCanvas()
    // window.addEventListener('resize', () => {
    //     resizeCanvas()
    //     // drawTriangle(50, 250)
    // })
}